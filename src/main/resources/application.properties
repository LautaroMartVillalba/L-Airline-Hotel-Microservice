#Conexión a la base de datos
spring.datasource.url= jdbc:postgresql://localhost:5432/airline
spring.datasource.username=postgres
spring.datasource.password=8462519573

#Configuración de JPA y Hibernate
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none

#Configuración del proyecto
spring.application.name=microservice-hotel
eureka.instance.instance-id=${spring.application.name}:${random.uuid}
server.port=8080
#FlyWay

spring.flyway.locations=classpath:/db/migration

#Eureka
#spring.cloud.config.discovery.enabled=true
#spring.cloud.config.discovery.serviceId=eureka-server
#management.info.nv.enabled=true
#eureka.client.enabled=true
#spring.config.import=optional:configserver:
